# Core data science stack
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
imbalanced-learn>=0.11

# Visualization (Phase 4 EDA)
matplotlib>=3.7
seaborn>=0.12

# Deep learning (Phase 6) — installed separately, see README.md
# On machines with limited disk, use the CPU-only wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Live capture (Phase 8)
scapy>=2.5

# Config + model persistence
pyyaml>=6.0
joblib>=1.3

# Alerts (Phase 9)
requests>=2.31

# Dashboard (Phase 10)
streamlit>=1.28
